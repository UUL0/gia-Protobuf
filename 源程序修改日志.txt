相关链接的代码并不能直接使用，可能是版本导致，也可能是数据版本不同

---------------------------修改处：核心myProtobuf.py
————从protobuf二进制文件解码：ParseProto
————从base64编码的protobuf二进制数据解码：ParseProtoFromBase64
————protobuf二进制解码：Decode

————编码核心：ReEncode
修改未知错误（可能是版本）
使字节流判断逻辑能正确解析文本、字节、数值类型

————解码核心：ParseData
修改数据类型未能正确解析的问题：原处理逻辑是通过messages->字符串->repeated一个个试错
原因是由于没有proto定义
从现有GIA数据集二进制解码分析比对
数据类型是有存入二进制序列的
所以可以这样：
通过递归判断变量关键，检测到messages是变量，判断变量的类型
有类型传入递归，递归层层根据类型进行正确的类型值解码

再次迭代：
宏观逻辑：
ParseData内，返回的局部变量类型作为当前层的局部变量类型，遇到03:01:Varint设置局部变量类型有效，
遇到elif wire_type == 0x02:，遇到递归时传入当前局部变量类型，要解析string和repeated数据时根据自身局部变量类型解析，解析后设置自身局部变量类型为无效值，程序完成时返回自身局部变量类型。

根据现有6.5版本GIA数据，修改解析数组时的逻辑，使其输出是正确的string或repeated



